'use strict';

import TempHeader from '../temp-header';

describe('TempHeader View', function() {

  beforeEach(() => {
    this.tempHeader = new TempHeader();
  });

  it('Should run a few assertions', () => {
    expect(this.tempHeader);
  });

});
