'use strict';

import OurResearch from '../our-research';

describe('OurResearch View', function() {

  beforeEach(() => {
    this.ourResearch = new OurResearch();
  });

  it('Should run a few assertions', () => {
    expect(this.ourResearch);
  });

});
